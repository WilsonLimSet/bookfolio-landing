---
phase: 06-social-and-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Bookfolio/Bookfolio/Features/Discover/DiscoverView.swift
  - Bookfolio/Bookfolio/Features/Discover/UserSearchView.swift
  - Bookfolio/Bookfolio/Features/Discover/DiscoverTab.swift
autonomous: true
---

<objective>
Build the discover users screen with user search and suggested users.

Purpose: Users need to find and follow other readers. The discover screen provides search by username and suggests active readers the user doesn't follow yet.
Output: DiscoverTab replacing placeholder, UserSearchView with debounced search, DiscoverView with suggested and recently active users.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key files:
@Bookfolio/Bookfolio/Models/Profile.swift — Profile model
@Bookfolio/Bookfolio/Services/ProfileService.swift — existing profile fetch, follow/unfollow
@Bookfolio/Bookfolio/Services/AuthService.swift — auth state
@Bookfolio/Bookfolio/Features/Profile/FollowButton.swift — existing follow button
@Bookfolio/Bookfolio/Navigation/AppRoute.swift — routes
@Bookfolio/Bookfolio/Navigation/AppRouter.swift — tab structure (check for discover/lists tab)

Web app discover behavior (discover/page.tsx):
- User search: ilike username query with 300ms debounce, limit 10
- Suggested users: most active (by book count) not already followed, top 12
- Recently active: from recent activity, not followed, up to 8
- Each user card: avatar, username, book count, follow button
- Queries:
  - Search: supabase.from("profiles").select("id, username").ilike("username", `%${query}%`).neq("id", currentUserId).limit(10)
  - Active users: supabase.from("user_books").select("user_id").limit(1000), then count per user
  - Recent activity: supabase.from("activity").select("user_id").order("created_at", desc).limit(50)

Existing iOS patterns:
- BookSearchView uses debounced search (cancel Task, sleep 300ms)
- ProfileService.follow/unfollow with activity logging
- FollowButton with optimistic updates
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UserSearchView and DiscoverView</name>
  <files>
    Bookfolio/Bookfolio/Features/Discover/UserSearchView.swift,
    Bookfolio/Bookfolio/Features/Discover/DiscoverView.swift
  </files>
  <action>
1. CREATE Features/Discover/UserSearchView.swift — debounced user search:

Properties:
- `@State private var query = ""`
- `@State private var results: [Profile] = []`
- `@State private var isSearching = false`
- `@State private var searchTask: Task<Void, Never>?`
- `let currentUserId: UUID`
- `let onSelect: (Profile) -> Void`

Layout:
- TextField with magnifyingglass icon, "Search users..." placeholder
- Debounced search (same pattern as BookSearchView): cancel previous task, sleep 300ms, then search
- Search query: `supabase.from("profiles").select().ilike("username", "%\(query)%").neq("id", currentUserId).limit(10)`
- Results: LazyVStack of user rows:
  - HStack: avatar circle (40pt), username (bold), Spacer, chevron.right
  - Tap → onSelect(profile)
- Empty state after search: "No users found"
- Clear button on TextField

2. CREATE Features/Discover/DiscoverView.swift — main discover screen:

Properties:
- `@EnvironmentObject var authService: AuthService`
- `@State private var suggestedUsers: [(profile: Profile, bookCount: Int)] = []`
- `@State private var isLoading = true`
- `let onSelectUser: (UUID) -> Void`

Layout (ScrollView):
- UserSearchView at top (search bar)
- Section "Suggested Readers":
  - If isLoading: ProgressView
  - LazyVStack of user cards:
    - HStack: avatar (40pt), VStack(username bold, "\(bookCount) books ranked" gray), Spacer, FollowButton
    - Tap user area → onSelectUser(profile.id)
  - If empty: "No suggestions yet" gray text

Data loading (.task):
- Guard authenticated
- Fetch following IDs: `supabase.from("follows").select("following_id").eq("follower_id", userId)`
- Fetch active users: `supabase.from("user_books").select("user_id").limit(1000)`
- Count books per user (Dictionary), exclude self and already followed
- Sort by count descending, take top 12 user IDs
- Fetch profiles for those IDs: `supabase.from("profiles").select().in("id", userIds)`
- Map to (profile, bookCount) tuples
- Set suggestedUsers and isLoading = false

IMPORTANT: Debounce pattern: cancel previous Task, create new Task with try await Task.sleep(nanoseconds: 300_000_000).
IMPORTANT: Use private Decodable row types for lightweight queries (just user_id from user_books, just following_id from follows).
IMPORTANT: Handle empty following list (new users see all suggestions).
  </action>
  <verify>UserSearchView compiles with debounced search. DiscoverView compiles with suggested users.</verify>
  <done>UserSearchView with debounced user search. DiscoverView with suggested readers by activity.</done>
</task>

<task type="auto">
  <name>Task 2: Create DiscoverTab and integrate</name>
  <files>Bookfolio/Bookfolio/Features/Discover/DiscoverTab.swift</files>
  <action>
1. Check the current tab structure — read AppRouter.swift to see what tabs exist and how they work.

2. CREATE Features/Discover/DiscoverTab.swift:

Look at how other tabs are structured (e.g., SearchTab, ProfileTab). Follow the same pattern.

```swift
struct DiscoverTab: View {
    @ObservedObject var router: TabRouter

    var body: some View {
        NavigationStack(path: $router.path) {
            DiscoverView { userId in
                router.push(.userProfile(userId: userId))
            }
            .navigationTitle("Discover")
            .withRouteDestinations()
        }
    }
}
```

3. Check if there's already a placeholder tab for Discover or Lists in AppRouter. If so, replace it.
   If the tab uses a different name (e.g., "Lists" placeholder), adapt the DiscoverTab to fit.

IMPORTANT: Read AppRouter.swift first to understand the tab structure and where Discover should go.
IMPORTANT: Don't break existing tabs. Only replace the appropriate placeholder.
IMPORTANT: DiscoverView's onSelectUser should navigate via the router to .userProfile.
  </action>
  <verify>
  - `xcodebuild -project Bookfolio/Bookfolio.xcodeproj -scheme Bookfolio -destination 'platform=iOS Simulator,name=iPhone 16 Pro,OS=18.6' build` succeeds
  - DiscoverTab shows in tab bar
  - User search works with debounce
  - Suggested users display with follow buttons
  </verify>
  <done>DiscoverTab integrated into tab bar with user search and suggested readers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] UserSearchView compiles with debounced search
- [ ] DiscoverView shows suggested users by activity
- [ ] DiscoverTab integrated into tab bar
- [ ] User search returns results from profiles table
- [ ] Suggested users exclude self and already followed
- [ ] Navigation to user profile works
- [ ] Project builds without errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- User search finds profiles by username
- Suggested users are active readers not yet followed
- Follow button works from discover screen
- Navigation to user profiles works
</success_criteria>

<output>
After completion, create `.planning/phases/06-social-and-notifications/06-02-SUMMARY.md`
</output>
