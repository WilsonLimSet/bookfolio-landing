---
phase: 07-lists-import-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Bookfolio/Bookfolio/Services/ListService.swift
  - Bookfolio/Bookfolio/Features/Lists/ListsView.swift
  - Bookfolio/Bookfolio/Features/Lists/ListDetailView.swift
  - Bookfolio/Bookfolio/Features/Lists/CreateListView.swift
  - Bookfolio/Bookfolio/Navigation/AppRoute.swift
  - Bookfolio/Bookfolio/Navigation/RouteDestination.swift
  - Bookfolio/Bookfolio/Features/Profile/ProfileView.swift
autonomous: true
---

<objective>
Build the book lists feature — service layer, browsable lists, list detail, create list, and profile integration.

Purpose: Book lists let users curate themed collections (e.g., "Best Sci-Fi 2025", "Beach Reads"). This is a core social feature — users can browse public lists and manage their own.
Output: ListService with CRUD, ListsView for browsing, ListDetailView, CreateListView, lists section in ProfileView.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key files:
@Bookfolio/Bookfolio/Models/BookList.swift — BookList model (id, userId, name, description, isPublic, createdAt, updatedAt)
@Bookfolio/Bookfolio/Models/BookListItem.swift — BookListItem model (id, listId, openLibraryKey, title, author, coverUrl, position, addedAt)
@Bookfolio/Bookfolio/Services/SupabaseService.swift — supabase singleton
@Bookfolio/Bookfolio/Services/ProfileService.swift — static methods pattern
@Bookfolio/Bookfolio/Navigation/AppRoute.swift — existing routes (has .listDetail placeholder)
@Bookfolio/Bookfolio/Navigation/RouteDestination.swift — .listDetail maps to PlaceholderDestination
@Bookfolio/Bookfolio/Features/Profile/ProfileView.swift — needs "Your Lists" section
@Bookfolio/Bookfolio/SharedComponents/BookCoverView.swift — reusable cover

Web app reference (lists/page.tsx):
- Browse public lists: supabase.from("book_lists").select("*, profiles(username, avatar_url), book_list_items(*)").eq("is_public", true).order("updated_at", descending: true).limit(20)
- User's lists: supabase.from("book_lists").select("*, book_list_items(*)").eq("user_id", userId).order("updated_at", descending: true)
- Create list: insert into book_lists (user_id, name, description, is_public)
- Delete list: delete from book_lists WHERE id
- List detail: fetch list with items ordered by position

Tab structure: 5 tabs (feed, search, profile, leaderboard, discover). Lists is NOT a tab — access from Profile and via .listDetail route. Add new routes: .myLists, .createList, .browseLists.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ListService</name>
  <files>Bookfolio/Bookfolio/Services/ListService.swift</files>
  <action>
CREATE Services/ListService.swift — enum with static methods:

Define private Decodable row types:
```swift
private struct ListWithItemsRow: Decodable {
    let id: UUID
    let userId: UUID
    let name: String
    let description: String?
    let isPublic: Bool
    let createdAt: Date
    let updatedAt: Date
    let bookListItems: [BookListItem]

    enum CodingKeys: String, CodingKey {
        case id, name, description
        case userId = "user_id"
        case isPublic = "is_public"
        case createdAt = "created_at"
        case updatedAt = "updated_at"
        case bookListItems = "book_list_items"
    }
}

private struct ListWithProfileRow: Decodable {
    let id: UUID
    let userId: UUID
    let name: String
    let description: String?
    let isPublic: Bool
    let createdAt: Date
    let updatedAt: Date
    let profiles: ProfileRow
    let bookListItems: [BookListItem]

    struct ProfileRow: Decodable {
        let username: String?
        let avatarUrl: String?
        enum CodingKeys: String, CodingKey {
            case username
            case avatarUrl = "avatar_url"
        }
    }

    enum CodingKeys: String, CodingKey {
        case id, name, description, profiles
        case userId = "user_id"
        case isPublic = "is_public"
        case createdAt = "created_at"
        case updatedAt = "updated_at"
        case bookListItems = "book_list_items"
    }
}
```

Methods:

a. `static func fetchPublicLists() async -> [(list: BookList, creatorName: String?, items: [BookListItem])]`:
   - Query: `supabase.from("book_lists").select("*, profiles(username, avatar_url), book_list_items(*)").eq("is_public", true).order("updated_at", ascending: false).limit(20)`
   - Decode as [ListWithProfileRow], map to tuple
   - Use try? — non-critical

b. `static func fetchUserLists(userId: UUID) async -> [(list: BookList, items: [BookListItem])]`:
   - Query: `supabase.from("book_lists").select("*, book_list_items(*)").eq("user_id", userId).order("updated_at", ascending: false)`
   - Decode as [ListWithItemsRow], map to tuple

c. `static func fetchListDetail(listId: UUID) async throws -> (list: BookList, items: [BookListItem], creatorName: String?)`:
   - Query: `supabase.from("book_lists").select("*, profiles(username, avatar_url), book_list_items(*)").eq("id", listId).limit(1)`
   - Decode as [ListWithProfileRow], take .first, throw if nil

d. `static func createList(userId: UUID, name: String, description: String?, isPublic: Bool) async throws -> BookList`:
   - Use private Encodable struct (no id, no timestamps — server-generated)
   - Insert and return created list

e. `static func deleteList(listId: UUID) async throws`:
   - Delete from book_lists WHERE id = listId
   - (book_list_items cascade-deleted by foreign key)

f. `static func addBookToList(listId: UUID, bookKey: String, title: String, author: String?, coverUrl: String?) async throws`:
   - Get current max position: query book_list_items WHERE list_id, order by position desc, limit 1
   - Insert new item with position = maxPosition + 1

g. `static func removeBookFromList(listId: UUID, bookKey: String) async throws`:
   - Delete from book_list_items WHERE list_id AND open_library_key

IMPORTANT: Use private Encodable insert structs (no server-generated fields like id, added_at).
IMPORTANT: CodingKeys for snake_case mapping on all private row types.
IMPORTANT: BookListItem model already exists — reuse it for item arrays.
  </action>
  <verify>ListService compiles with all CRUD methods.</verify>
  <done>ListService with fetch, create, delete lists, add/remove books.</done>
</task>

<task type="auto">
  <name>Task 2: Create ListsView, ListDetailView, CreateListView, wire routes, add to ProfileView</name>
  <files>
    Bookfolio/Bookfolio/Features/Lists/ListsView.swift,
    Bookfolio/Bookfolio/Features/Lists/ListDetailView.swift,
    Bookfolio/Bookfolio/Features/Lists/CreateListView.swift,
    Bookfolio/Bookfolio/Navigation/AppRoute.swift,
    Bookfolio/Bookfolio/Navigation/RouteDestination.swift,
    Bookfolio/Bookfolio/Features/Profile/ProfileView.swift
  </files>
  <action>
1. ADD to AppRoute.swift:
   - `.myLists(userId: UUID)` — view user's lists
   - `.createList` — create new list
   - `.browseLists` — browse public lists

2. CREATE Features/Lists/ListsView.swift — browsable public lists:

Properties:
- `@State private var publicLists: [(list: BookList, creatorName: String?, items: [BookListItem])] = []`
- `@State private var isLoading = true`
- `let onSelectList: (UUID) -> Void`

Layout (ScrollView):
- If isLoading: ProgressView
- Section "Public Lists":
  - LazyVStack of list cards:
    - VStack: list name (bold), creator name (gray), description (2 lines, gray)
    - HStack of first 4 book covers (BookCoverView, 50x75) with "+N more" if > 4
    - Tap → onSelectList(list.id)
  - If empty: "No public lists yet" gray text

Data loading (.task):
- Call ListService.fetchPublicLists()
- Set publicLists, isLoading = false

3. CREATE Features/Lists/ListDetailView.swift:

Properties:
- `let listId: UUID`
- `@EnvironmentObject var authService: AuthService`
- `@State private var list: BookList?`
- `@State private var items: [BookListItem] = []`
- `@State private var creatorName: String?`
- `@State private var isLoading = true`
- `@State private var showDeleteConfirmation = false`
- `@Environment(\.dismiss) var dismiss`

Layout (ScrollView):
- Header: list name (title), creator (gray), description
- isPublic badge: "Public" or "Private" pill
- If owner: Edit/Delete buttons
  - Delete with .confirmationDialog
- LazyVGrid (2 columns) of items:
  - BookCoverView (100x150), title below, author gray
  - Tap → navigate to .bookDetail(bookKey: item.openLibraryKey)
- If empty: "No books in this list yet"

Navigation: need `let onSelectBook: (String) -> Void` callback

Delete action: Call ListService.deleteList, dismiss

Data loading (.task): ListService.fetchListDetail(listId:)

4. CREATE Features/Lists/CreateListView.swift:

Properties:
- `@EnvironmentObject var authService: AuthService`
- `@State private var name = ""`
- `@State private var description = ""`
- `@State private var isPublic = true`
- `@State private var isSaving = false`
- `@Environment(\.dismiss) var dismiss`
- `let onCreated: ((BookList) -> Void)?`

Layout (Form):
- TextField "List name" (required)
- TextEditor "Description" (optional)
- Toggle "Make public"
- Save button (disabled if name empty or isSaving)

Save action:
- Guard authenticated
- Call ListService.createList(...)
- On success: call onCreated, dismiss

5. UPDATE RouteDestination.swift:
- Replace `.listDetail` placeholder with `ListDetailView(listId: listId, onSelectBook: { ... })`
  - onSelectBook should NOT navigate (ListDetailView is already in a NavigationStack) — use NavigationLink(value:) inside ListDetailView instead
  - Actually: just use `ListDetailView(listId: listId)` and put NavigationLink inside the view
- Add `.myLists` → `MyListsView(userId: userId)` (see below)
- Add `.createList` → `CreateListView(onCreated: nil)`
- Add `.browseLists` → `ListsView { listId in ... }` with navigation

Wait — for navigation from within ListDetailView, items should use NavigationLink(value: .bookDetail(bookKey:)). That works because ListDetailView is inside a NavigationStack with .withRouteDestinations().

For ListDetailView, don't use callback — use NavigationLink directly inside the view.

6. CREATE a small MyListsView (can be in ListsView.swift or separate):

Properties:
- `let userId: UUID`
- `@EnvironmentObject var authService: AuthService`
- `@State private var lists: [(list: BookList, items: [BookListItem])] = []`
- `@State private var isLoading = true`

Layout:
- If owner: toolbar button "+" to create list (NavigationLink to .createList)
- LazyVStack of user's lists with cover previews
- Each list card taps to .listDetail(listId:)
- If empty: "No lists yet — create your first!"

7. UPDATE ProfileView.swift:
- After the followers/following HStack (line ~103), add a "Lists" section:
  - If isOwner: NavigationLink to .myLists(userId: userId) — "Your Lists →"
  - Else: NavigationLink to .myLists(userId: userId) — "[username]'s Lists →"
  - Show count if available (optional — can skip for simplicity)

IMPORTANT: ListDetailView uses NavigationLink(value: .bookDetail(bookKey:)) for book navigation — no callback needed.
IMPORTANT: Read ListsTab.swift — it's the old placeholder. Can delete it or replace its content.
IMPORTANT: foregroundColor for conditional colors (iOS 16 compat).
IMPORTANT: Add all new Swift files to Xcode project.pbxproj.
  </action>
  <verify>
  - `xcodebuild -project Bookfolio/Bookfolio.xcodeproj -scheme Bookfolio -destination 'platform=iOS Simulator,name=iPhone 16 Pro,OS=18.6' build` succeeds
  - ListsView shows public lists
  - ListDetailView shows list with book grid
  - CreateListView form works
  - Profile has "Your Lists" link
  - Routes all wired
  </verify>
  <done>Complete lists feature with browse, detail, create, profile integration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ListService compiles with all CRUD methods
- [ ] ListsView shows public lists with cover previews
- [ ] ListDetailView shows list info and book grid
- [ ] CreateListView creates new lists
- [ ] ProfileView has lists link
- [ ] New routes wired in AppRoute and RouteDestination
- [ ] Project builds without errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Public lists browsable
- List detail shows books
- Create list works
- Profile links to user's lists
- Navigation works throughout
</success_criteria>

<output>
After completion, create `.planning/phases/07-lists-import-polish/07-01-SUMMARY.md`
</output>
