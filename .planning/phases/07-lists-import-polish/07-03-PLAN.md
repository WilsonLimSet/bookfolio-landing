---
phase: 07-lists-import-polish
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - Bookfolio/Bookfolio/Features/Book/BookDetailView.swift
  - Bookfolio/Bookfolio/Features/Lists/AddToListSheet.swift
  - Bookfolio/Bookfolio/Features/Feed/FeedItemView.swift
  - Bookfolio/Bookfolio/Features/Profile/ProfileView.swift
  - Bookfolio/Bookfolio/Features/Discover/DiscoverView.swift
autonomous: true
---

<objective>
Polish the app — add "Add to List" button on BookDetailView, pull-to-refresh on key screens, haptic feedback on social actions, and error/empty state improvements.

Purpose: Polish transforms a functional app into a delightful one. Pull-to-refresh, haptics on interactions, and the "Add to List" integration complete the feature loop.
Output: AddToListSheet on book detail, pull-to-refresh on feed/profile/discover/leaderboard, haptic feedback on social actions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key files:
@Bookfolio/Bookfolio/Features/Book/BookDetailView.swift — add "Add to List" button
@Bookfolio/Bookfolio/Services/ListService.swift — fetchUserLists, addBookToList (from 07-01)
@Bookfolio/Bookfolio/Features/Feed/FeedItemView.swift — like button haptics
@Bookfolio/Bookfolio/Features/Profile/ProfileView.swift — pull-to-refresh
@Bookfolio/Bookfolio/Features/Discover/DiscoverView.swift — pull-to-refresh

Existing haptic patterns:
- RankingFlow: UIImpactFeedbackGenerator(.medium) on comparisons
- TierStep: UIImpactFeedbackGenerator(.medium) on tier selection
- CompareStep: UINotificationFeedbackGenerator().notificationOccurred(.success) on completion

Pull-to-refresh: SwiftUI .refreshable modifier on ScrollView (available iOS 15+)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AddToListSheet and integrate into BookDetailView</name>
  <files>
    Bookfolio/Bookfolio/Features/Lists/AddToListSheet.swift,
    Bookfolio/Bookfolio/Features/Book/BookDetailView.swift
  </files>
  <action>
1. CREATE Features/Lists/AddToListSheet.swift:

Properties:
- `let bookKey: String`
- `let title: String`
- `let author: String?`
- `let coverUrl: String?`
- `@EnvironmentObject var authService: AuthService`
- `@State private var userLists: [(list: BookList, items: [BookListItem])] = []`
- `@State private var isLoading = true`
- `@State private var showCreateList = false`
- `@Environment(\.dismiss) var dismiss`

Layout (NavigationStack):
- NavigationTitle "Add to List"
- Toolbar: dismiss button, "+" button to create new list
- If isLoading: ProgressView
- LazyVStack of user's lists:
  - HStack: list name, Spacer, checkmark if book already in list
  - Tap: if not in list → add book via ListService.addBookToList, show checkmark
  - If already in list → remove via ListService.removeBookFromList, remove checkmark
  - Haptic feedback (UIImpactFeedbackGenerator .light) on add/remove
- If no lists: "No lists yet" + "Create List" button
- Sheet for CreateListView when showCreateList = true

Data loading (.task):
- Guard authenticated
- Fetch user lists via ListService.fetchUserLists(userId:)
- Set userLists and isLoading = false

2. UPDATE BookDetailView.swift:
- Add `@State private var showAddToList = false`
- In actionButtonsSection(), after the WantToRead/CurrentlyReading buttons, add:
  ```swift
  Button {
      showAddToList = true
  } label: {
      Label("Add to List", systemImage: "list.bullet")
          .font(.subheadline)
          .frame(maxWidth: .infinity)
          .padding(.vertical, 8)
  }
  .buttonStyle(.bordered)
  ```
- Add .sheet(isPresented: $showAddToList) with AddToListSheet

IMPORTANT: Read BookDetailView.swift first — understand the full structure before modifying.
IMPORTANT: AddToListSheet uses NavigationStack (it's presented as a sheet, needs its own nav).
IMPORTANT: Haptic on add/remove for tactile feedback.
  </action>
  <verify>AddToListSheet compiles. BookDetailView shows "Add to List" button.</verify>
  <done>AddToListSheet with list toggle. BookDetailView has "Add to List" button.</done>
</task>

<task type="auto">
  <name>Task 2: Add pull-to-refresh and haptic feedback polish</name>
  <files>
    Bookfolio/Bookfolio/Features/Feed/FeedItemView.swift,
    Bookfolio/Bookfolio/Features/Profile/ProfileView.swift,
    Bookfolio/Bookfolio/Features/Discover/DiscoverView.swift
  </files>
  <action>
Read each file first, then add:

1. **Pull-to-refresh** — add .refreshable modifier to ScrollView in these views:

a. ProfileView.swift:
   - Find the ScrollView in the body
   - Add .refreshable { await loadProfile() }
   - Make sure loadProfile() can be called again (reset isLoading state properly)

b. DiscoverView.swift:
   - Find the ScrollView
   - Add .refreshable to reload suggested users
   - The reload function should re-fetch following IDs and active users

NOTE: FeedItemView is a row, not a screen — find the actual Feed screen (likely FeedTab or a FeedView) and add .refreshable there if it has a ScrollView. If feed uses List, .refreshable works on List too.

Read the Feed files to find the right place:
- FeedTab.swift → check if it wraps a ScrollView/List
- If FeedTabs.swift exists, check there

2. **Haptic feedback on social actions:**

a. FeedItemView.swift — like button:
   - Find the like button action
   - Add UIImpactFeedbackGenerator(style: .light).impactOccurred() when toggling like

b. FollowButton (if accessible — read it first):
   - Add UIImpactFeedbackGenerator(style: .light).impactOccurred() on follow/unfollow

c. CommentSectionView — post comment:
   - Add UINotificationFeedbackGenerator().notificationOccurred(.success) on successful post

NOTE: Only add haptics where they don't already exist. Check first.

3. **Verify build:**
   - Run xcodebuild to ensure all changes compile

IMPORTANT: Read each file before modifying — don't break existing functionality.
IMPORTANT: .refreshable is async — the closure should await the data reload.
IMPORTANT: UIImpactFeedbackGenerator needs `import UIKit`.
IMPORTANT: Don't add haptics to views that already have them (ranking flow already does).
  </action>
  <verify>
  - `xcodebuild -project Bookfolio/Bookfolio.xcodeproj -scheme Bookfolio -destination 'platform=iOS Simulator,name=iPhone 16 Pro,OS=18.6' build` succeeds
  - Pull-to-refresh works on profile and discover
  - Haptic feedback on like, follow, and comment post
  </verify>
  <done>Pull-to-refresh on key screens. Haptic feedback on social actions. AddToList on BookDetail.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] AddToListSheet compiles with list management
- [ ] BookDetailView has "Add to List" button with sheet
- [ ] Pull-to-refresh on profile, discover
- [ ] Haptic feedback on like, follow, comment actions
- [ ] Project builds without errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Add to List flow works from book detail
- Pull-to-refresh refreshes data on key screens
- Haptic feedback on all social interactions
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/07-lists-import-polish/07-03-SUMMARY.md`
</output>
