---
phase: 04-book-discovery
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - Bookfolio/Bookfolio/Features/Search/SearchTab.swift
  - Bookfolio/Bookfolio/Features/Search/BookSearchView.swift
  - Bookfolio/Bookfolio/Features/Book/BookDetailView.swift
  - Bookfolio/Bookfolio/Features/Book/EditionPickerView.swift
  - Bookfolio/Bookfolio/SharedComponents/BookCoverView.swift
  - Bookfolio/Bookfolio/Navigation/RouteDestination.swift
autonomous: true
---

<objective>
Build the book search screen and book detail screen with description, editions, community ratings, and author info.

Purpose: Users need to discover books (search) and see detailed info before ranking/reading. The book detail page is the central hub for all book actions.
Output: SearchTab with live search, BookDetailView with full book info, EditionPickerView for browsing covers, BookCoverView shared component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-book-discovery/04-01-PLAN.md

Key files:
@Bookfolio/Bookfolio/Services/OpenLibraryService.swift — created in Plan 01 with searchBooks, getBookDetails, getEditions
@Bookfolio/Bookfolio/Models/OpenLibraryTypes.swift — OL Codable types
@Bookfolio/Bookfolio/Models/UserBook.swift — UserBook for community ratings
@Bookfolio/Bookfolio/Models/Profile.swift — Profile for reviewer info
@Bookfolio/Bookfolio/Features/Search/SearchTab.swift — current placeholder to replace
@Bookfolio/Bookfolio/Navigation/AppRoute.swift — .bookDetail(bookKey:) route exists
@Bookfolio/Bookfolio/Navigation/RouteDestination.swift — .bookDetail currently placeholder
@Bookfolio/Bookfolio/Navigation/TabRouter.swift — per-tab nav
@Bookfolio/Bookfolio/Services/AuthService.swift — auth state

Web app book detail shows:
- Book cover (large), title, author, translator, year, page count
- User's own rating badge if ranked: "#X in {category}" with tier color
- Description (may be long — truncate with "Show more")
- Topics/subjects (up to 6 pills)
- Community ratings: average score + count + reviewer cards (avatar, username, score, review preview)
- Action buttons: "Add to my list" (rank), "Currently Reading", "Want to Read" (these go in Plan 03)

Web app search:
- Search bar with debounce (300ms)
- Results: cover thumbnail, title, author, year
- In-memory cache with 5-min TTL (optional for v1)
- Max 8 results displayed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BookCoverView, BookSearchView, and SearchTab</name>
  <files>
    Bookfolio/Bookfolio/SharedComponents/BookCoverView.swift,
    Bookfolio/Bookfolio/Features/Search/BookSearchView.swift,
    Bookfolio/Bookfolio/Features/Search/SearchTab.swift
  </files>
  <action>
1. CREATE SharedComponents/BookCoverView.swift — reusable book cover with loading/fallback:

Properties:
- `let coverUrl: String?`
- `let size: CGSize` (default CGSize(width: 80, height: 120))

Layout:
- AsyncImage loading cover URL
- Loading: Rectangle with book.fill icon, gray background, shimmer optional
- Failure/nil: Rectangle with book title abbreviation or book.fill icon
- Success: Image resizable, aspect fill, clipped to size
- Applied: cornerRadius 8, shadow(radius: 2)

Keep it simple — no spine/page edge effects for v1.

2. CREATE BookSearchView.swift — search results list:

Properties:
- `@State private var query = ""`
- `@State private var results: [BookSearchItem] = []`
- `@State private var isSearching = false`
- `@State private var searchTask: Task<Void, Never>?`
- `let onSelect: (BookSearchItem) -> Void`

Layout:
- TextField with magnifyingglass icon, "Search books..." placeholder
- Debounced search: on query change, cancel previous searchTask, wait 300ms, then call OpenLibraryService.searchBooks
- Results: LazyVStack of HStack rows:
  - BookCoverView(coverUrl:, size: CGSize(width: 40, height: 60))
  - VStack: title (bold), author (gray), year (gray, small)
  - Tap row → onSelect(item)
- Loading: ProgressView below search bar
- Empty state (after search): "No books found" text
- Clear button on TextField

IMPORTANT: Debounce pattern — cancel previous Task, create new Task with `try await Task.sleep(nanoseconds: 300_000_000)`, then search. If cancelled (CancellationError), silently return.

3. REPLACE SearchTab.swift — integrate search with navigation:

```swift
struct SearchTab: View {
    @ObservedObject var router: TabRouter

    var body: some View {
        NavigationStack(path: $router.path) {
            BookSearchView { item in
                router.push(.bookDetail(bookKey: item.key))
            }
            .navigationTitle("Search")
            .withRouteDestinations()
        }
    }
}
```
  </action>
  <verify>SearchTab shows search bar. BookSearchView debounces and shows results. BookCoverView renders covers.</verify>
  <done>SearchTab with live book search, debounced results, and navigation to book detail.</done>
</task>

<task type="auto">
  <name>Task 2: Build BookDetailView, EditionPickerView, and wire routes</name>
  <files>
    Bookfolio/Bookfolio/Features/Book/BookDetailView.swift,
    Bookfolio/Bookfolio/Features/Book/EditionPickerView.swift,
    Bookfolio/Bookfolio/Navigation/RouteDestination.swift
  </files>
  <action>
1. CREATE Features/Book/BookDetailView.swift:

Properties:
- `let bookKey: String` (OpenLibrary work key, e.g., "/works/OL12345W")
- `@EnvironmentObject var authService: AuthService`
- `@State private var bookDetails: BookDetails?`
- `@State private var userBook: UserBook?` (current user's rating if exists)
- `@State private var communityRatings: [UserBook] = []`
- `@State private var reviewerProfiles: [UUID: Profile] = [:]`
- `@State private var isLoading = true`
- `@State private var showEditionPicker = false`
- `@State private var isDescriptionExpanded = false`

Layout (ScrollView):

Section 1 — Header:
- BookCoverView(coverUrl:, size: CGSize(width: 160, height: 240)) centered
- Title (large, bold)
- Author (medium, gray)
- HStack pills: year pill, page count pill, translator (if present)

Section 2 — User's Rating (if userBook != nil):
- Badge: "#\(userBook.rankPosition) in \(userBook.category.rawValue.capitalized)"
- Tier color indicator (green/yellow/red)
- "Edit" button → navigate to .rankBook route

Section 3 — Action Buttons:
- For now, show placeholder buttons that will be wired in Plan 03:
  - If not ranked: "Add to my list" button (disabled/placeholder)
  - "Currently Reading" toggle (placeholder)
  - "Want to Read" toggle (placeholder)
- Add TODO comments noting Plan 03 will implement these

Section 4 — Description:
- Text showing bookDetails.description
- If description > 200 chars: show truncated with "Show more" button
- isDescriptionExpanded toggles full text

Section 5 — Subjects:
- Horizontal ScrollView of subject pills (rounded rect, gray background, small text)
- Up to 6 subjects

Section 6 — Community Ratings:
- "Community" header with average score + count (e.g., "8.2 avg · 15 ratings")
- LazyVStack of reviewer cards:
  - HStack: avatar circle (30pt), username, Spacer, score pill (colored)
  - If review_text: Text preview (2 lines)
  - Tap → navigate to .userProfile(userId:) or .reviewDetail(reviewId:)

Data loading (.task modifier):
1. Fetch book details: `OpenLibraryService.getBookDetails(workKey: bookKey)`
2. If authenticated, fetch in parallel:
   a. User's rating: `supabase.from("user_books").select().eq("user_id", ...).eq("open_library_key", bookKey).maybeSingle()` — use try? (may not exist)
   b. Community ratings: `supabase.from("user_books").select().eq("open_library_key", bookKey).order("score", ascending: false).limit(100)`
   c. Reviewer profiles: fetch profiles for unique userIds from community ratings

"Browse Editions" button → sets showEditionPicker = true.

IMPORTANT: The bookKey from AppRoute is the OpenLibrary work key (e.g., "/works/OL12345W"). Use this for API calls and as `open_library_key` in Supabase queries.
IMPORTANT: `maybeSingle()` in supabase-swift returns nil if no row found instead of throwing. Use this for user's rating query.
IMPORTANT: Action buttons are PLACEHOLDERS in this plan — Plan 03 will implement the real want-to-read/currently-reading toggles.

2. CREATE Features/Book/EditionPickerView.swift:

Properties:
- `let workKey: String`
- `@State private var editions: [BookEditionItem] = []`
- `@State private var isLoading = true`
- `let onSelect: (BookEditionItem) -> Void`
- `@Environment(\.dismiss) var dismiss`

Present as .sheet from BookDetailView.

Layout:
- NavigationView with "Editions" title and "Done" dismiss button
- LazyVGrid (3 columns):
  - Each cell: BookCoverView + publisher/year label below
  - Tap → onSelect(edition), dismiss

Load: `OpenLibraryService.getEditions(workKey: workKey)`

3. UPDATE RouteDestination.swift:
- Map `.bookDetail(let bookKey)` → `BookDetailView(bookKey: bookKey)`
- Keep all other route mappings unchanged

IMPORTANT: Read the current RouteDestination.swift first to preserve existing mappings.
  </action>
  <verify>
  - `xcodebuild -project Bookfolio/Bookfolio.xcodeproj -scheme Bookfolio -destination 'platform=iOS Simulator,name=iPhone 16 Pro,OS=18.6' build` succeeds
  - BookDetailView shows book info from OpenLibrary
  - EditionPickerView shows edition grid
  - .bookDetail route wired to real view
  </verify>
  <done>BookDetailView with cover, description, subjects, community ratings. EditionPickerView with cover grid. SearchTab with live search. .bookDetail route wired.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] SearchTab shows live search with debounce
- [ ] BookDetailView loads and displays book details
- [ ] EditionPickerView shows edition grid
- [ ] Community ratings display with profiles
- [ ] .bookDetail route wired in RouteDestination
- [ ] Project builds without errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Search finds books via OpenLibrary API
- Book detail shows complete info (cover, description, subjects, ratings)
- Edition picker displays available covers
- Navigation from search → book detail works
</success_criteria>

<output>
After completion, create `.planning/phases/04-book-discovery/04-02-SUMMARY.md`
</output>
