---
phase: 05-ranking-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Bookfolio/Bookfolio/Services/RankingService.swift
  - Bookfolio/Bookfolio/Features/Ranking/RankingFlowView.swift
  - Bookfolio/Bookfolio/Features/Ranking/CoverSelectionStep.swift
  - Bookfolio/Bookfolio/Navigation/RouteDestination.swift
autonomous: true
---

<objective>
Create the RankingFlowView shell with step management, RankingService for data operations, and the first step (cover selection). Wire the .rankBook route.

Purpose: Establish the multi-step ranking flow container and data layer that all subsequent steps will build upon. The cover selection step is the entry point where users pick their preferred book cover.
Output: RankingFlowView with step navigation, RankingService with prefetch and RPC methods, CoverSelectionStep with edition grid, .rankBook route wired.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key files:
@Bookfolio/Bookfolio/Models/UserBook.swift — UserBook, RankBookParams, BookCategory, BookTier
@Bookfolio/Bookfolio/Models/Activity.swift — NewActivity with action_type "ranked"
@Bookfolio/Bookfolio/Services/OpenLibraryService.swift — getEditions(), detectCategory(), fetchWorkSubjects()
@Bookfolio/Bookfolio/Services/BookActionService.swift — BookMetadata struct
@Bookfolio/Bookfolio/Features/Book/EditionPickerView.swift — existing edition grid pattern
@Bookfolio/Bookfolio/Features/Book/BookDetailView.swift — NavigationLink to .rankBook
@Bookfolio/Bookfolio/Navigation/AppRoute.swift — .rankBook(bookKey:, title:, author:, coverUrl:) route
@Bookfolio/Bookfolio/Navigation/RouteDestination.swift — placeholder to replace
@Bookfolio/Bookfolio/SharedComponents/BookCoverView.swift — reusable cover component

Web app reference (RankingFlow.tsx):
- Steps: cover → category → tier → compare → review → saving
- Prefetches user books for both categories in parallel on mount
- Binary search within same tier for positioning
- rank_book RPC params: p_user_id, p_title, p_author, p_cover_url, p_open_library_key, p_category, p_tier, p_rank_position, p_review_text, p_finished_at, p_existing_entry_id
- RPC returns { score: Double }
- After save: log activity with action_type "ranked", book_score from RPC result
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RankingService and RankingFlowView shell</name>
  <files>
    Bookfolio/Bookfolio/Services/RankingService.swift,
    Bookfolio/Bookfolio/Features/Ranking/RankingFlowView.swift
  </files>
  <action>
1. CREATE Services/RankingService.swift — enum with static methods for ranking data:

```swift
enum RankingStep: Int, CaseIterable {
    case cover, category, tier, compare, review, saving
}
```

Methods:

a. `static func prefetchUserBooks(userId: UUID) async -> [String: [UserBook]]`:
   - Fetch fiction and nonfiction user_books in parallel (async let)
   - Query: `supabase.from("user_books").select().eq("user_id", userId).eq("category", "fiction").order("rank_position")` (same for nonfiction)
   - Return dictionary: ["fiction": [...], "nonfiction": [...]]
   - Use try? for each — return empty arrays on failure

b. `static func rankBook(params: RankBookParams) async throws -> Double`:
   - Call: `supabase.rpc("rank_book", params: params)`
   - RankBookParams already exists in UserBook.swift
   - Parse response to get score: decode as `[String: Double]` and return `result["score"] ?? 0`
   - IMPORTANT: Check what rank_book returns — it may return a single row. Try decoding as `PostgrestResponse` first.
   - Actually, supabase-swift rpc returns the raw response. Use: `try await supabase.rpc("rank_book", params: params).execute().value` — need to figure out the exact return type. The web app gets `{ score: number }`. Try `struct RankBookResult: Decodable { let score: Double }` and decode as single value.

c. `static func logRankingActivity(userId: UUID, book: BookMetadata, coverUrl: String?, score: Double, category: BookCategory) async`:
   - Fire-and-forget pattern (called with Task { try? await })
   - Insert NewActivity: action_type = .ranked, book fields from metadata, book_score = score, book_category = category.rawValue

2. CREATE Features/Ranking/RankingFlowView.swift — the multi-step ranking container:

Properties:
- `let bookKey: String`
- `let title: String`
- `let author: String?`
- `let coverUrl: String?`
- `let existingEntry: UserBook?` (nil for new ranking, set for re-ranking)
- `@EnvironmentObject var authService: AuthService`
- `@Environment(\.dismiss) var dismiss`
- `@State private var currentStep: RankingStep = .cover`
- `@State private var selectedCover: String?`
- `@State private var category: BookCategory?`
- `@State private var categoryAutoDetected = false`
- `@State private var tier: BookTier?`
- `@State private var userBooksCache: [String: [UserBook]] = [:]`
- `@State private var loadingBooks = true`
- `@State private var finalPosition: Int?`
- `@State private var reviewText = ""`
- `@State private var finishedAt: Date = Date()`
- `@State private var isSaving = false`
- `@State private var editions: [BookEditionItem] = []`
- `@State private var loadingEditions = true`

Layout:
- NavigationStack wrapping the flow
- VStack with:
  - Progress bar: `ProgressView(value: Double(currentStep.rawValue), total: Double(RankingStep.allCases.count - 1))` styled with accent color
  - Step content via switch on currentStep (placeholder for steps not yet built)
  - Back button (if not on first step) and forward/done button

Step navigation helpers:
- `func goToStep(_ step: RankingStep)` with animation
- `func goBack()` — decrement step
- `func goForward()` — increment step

Data loading in .task:
- Load editions: `OpenLibraryService.getEditions(workKey: bookKey)`
- Set selectedCover to coverUrl param or first edition cover
- If authenticated, prefetch user books: `RankingService.prefetchUserBooks(userId:)`
- If not existingEntry, auto-detect category: `OpenLibraryService.fetchWorkSubjects(workKey:)` → `OpenLibraryService.detectCategory(subjects:)`

For now, only the cover step is real. Other steps show placeholder Text views.

IMPORTANT: Initialize selectedCover from the coverUrl parameter passed from BookDetailView.
IMPORTANT: If existingEntry is provided (re-ranking), pre-fill category, tier from existing values.
IMPORTANT: Filter out current book from userBooksCache (exclude matching open_library_key).
  </action>
  <verify>RankingFlowView compiles with step enum and state management. RankingService compiles.</verify>
  <done>RankingFlowView shell with progress bar, step switching, data prefetching. RankingService with prefetch and RPC methods.</done>
</task>

<task type="auto">
  <name>Task 2: Create CoverSelectionStep and wire route</name>
  <files>
    Bookfolio/Bookfolio/Features/Ranking/CoverSelectionStep.swift,
    Bookfolio/Bookfolio/Navigation/RouteDestination.swift
  </files>
  <action>
1. CREATE Features/Ranking/CoverSelectionStep.swift:

Properties:
- `let editions: [BookEditionItem]`
- `let isLoading: Bool`
- `@Binding var selectedCover: String?`
- `let onNext: () -> Void`

Layout:
- VStack with:
  - "Choose a cover" title (large, bold)
  - "Select the edition cover you prefer" subtitle (gray)
  - If isLoading: ProgressView centered
  - Else: ScrollView with LazyVGrid (3 columns, spacing 12):
    - Each edition: BookCoverView with size CGSize(width: 100, height: 150)
    - Selected edition has blue border (3pt) and checkmark overlay
    - Tap → set selectedCover to edition.coverUrl
  - Spacer
  - "Next" button (large, filled, blue) → onNext()
  - Button disabled if selectedCover is nil

Style: Match the existing EditionPickerView grid pattern but with selection state.

2. UPDATE RouteDestination.swift:
- Replace the .rankBook placeholder with:
  ```swift
  case .rankBook(let bookKey, let title, let author, let coverUrl):
      RankingFlowView(bookKey: bookKey, title: title, author: author, coverUrl: coverUrl, existingEntry: nil)
  ```

IMPORTANT: Read RouteDestination.swift first to preserve all existing route mappings.
IMPORTANT: RankingFlowView doesn't handle existingEntry navigation yet — pass nil for now.
  </action>
  <verify>
  - `xcodebuild -project Bookfolio/Bookfolio.xcodeproj -scheme Bookfolio -destination 'platform=iOS Simulator,name=iPhone 16 Pro,OS=18.6' build` succeeds
  - CoverSelectionStep shows edition grid with selection
  - .rankBook route wired to RankingFlowView
  </verify>
  <done>CoverSelectionStep with edition grid and selection. .rankBook route wired to real RankingFlowView.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] RankingService compiles with prefetch and RPC methods
- [ ] RankingFlowView shows progress bar and step switching
- [ ] CoverSelectionStep shows edition grid with selection state
- [ ] .rankBook route navigates to RankingFlowView
- [ ] Data prefetching fires on load (editions + user books)
- [ ] Project builds without errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- RankingFlowView container with step management works
- Cover selection step shows editions and allows selection
- Navigation from BookDetailView to ranking flow works
- User books prefetched for comparison step
</success_criteria>

<output>
After completion, create `.planning/phases/05-ranking-flow/05-01-SUMMARY.md`
</output>
